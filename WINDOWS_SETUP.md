# Руководство по настройке Jotnal для Windows команды

Полное руководство по настройке, сборке и запуску проекта Jotnal на Windows.

## 📋 Содержание

- [Требования](#требования)
- [Установка инструментов](#установка-инструментов)
- [Сборка проекта](#сборка-проекта)
- [Запуск приложения](#запуск-приложения)
- [Решение проблем](#решение-проблем)

---

## Требования

### Минимальные требования
- Windows 10 или выше (x64)
- 4 GB RAM
- 500 MB свободного места на диске

### Необходимое ПО
- Go 1.24.7 или выше
- Git для Windows
- GCC компилятор (MinGW-w64) для CGO
- PowerShell 5.1 или выше (встроен в Windows 10/11)

---

## Установка инструментов

### 1. Установка Go

**Вариант A: Через официальный установщик (рекомендуется)**

1. Скачайте Go с официального сайта: https://go.dev/dl/
2. Выберите файл вида `go1.24.x.windows-amd64.msi`
3. Запустите установщик и следуйте инструкциям
4. После установки откройте PowerShell и проверьте:
   ```powershell
   go version
   ```

**Вариант B: Через winget (Windows Package Manager)**

```powershell
winget install GoLang.Go
```

**Вариант C: Через Chocolatey**

```powershell
choco install golang
```

### 2. Установка Git

**Вариант A: Официальный установщик**

1. Скачайте с https://git-scm.com/download/win
2. Запустите установщик с настройками по умолчанию

**Вариант B: Через winget**

```powershell
winget install Git.Git
```

**Вариант C: Через Chocolatey**

```powershell
choco install git
```

Проверка установки:
```powershell
git --version
```

### 3. Установка GCC (MinGW-w64)

GCC необходим для компиляции CGO зависимостей (SQLCipher).

**Вариант A: Через MSYS2 (рекомендуется)**

1. Скачайте MSYS2: https://www.msys2.org/
2. Установите MSYS2
3. Откройте MSYS2 MSYS и выполните:
   ```bash
   pacman -Syu
   pacman -S mingw-w64-x86_64-gcc
   ```
4. Добавьте в PATH: `C:\msys64\mingw64\bin`
   - Откройте "Система" → "Дополнительные параметры системы" → "Переменные среды"
   - В "Системные переменные" найдите `Path` и нажмите "Изменить"
   - Добавьте новую запись: `C:\msys64\mingw64\bin`
   - Нажмите OK

**Вариант B: Через Chocolatey**

```powershell
choco install mingw
```

**Вариант C: TDM-GCC (простая альтернатива)**

1. Скачайте TDM-GCC: https://jmeubank.github.io/tdm-gcc/
2. Установите "MinGW-w64 based" версию
3. Выберите "Add to PATH" во время установки

**Проверка установки GCC:**

```powershell
gcc --version
```

Должна появиться версия GCC (например, `gcc (MinGW-w64) 13.2.0`).

### 4. Настройка переменных окружения

Убедитесь, что следующие пути добавлены в PATH:
- `C:\Program Files\Go\bin` (Go)
- `C:\msys64\mingw64\bin` (GCC через MSYS2) или путь к TDM-GCC
- `%USERPROFILE%\go\bin` (Go пакеты)

Для проверки PATH:
```powershell
$env:PATH -split ';'
```

---

## Сборка проекта

### 1. Клонирование репозитория

```powershell
git clone https://github.com/deldim-kam/Jotnal.git
cd Jotnal
```

### 2. Проверка зависимостей

```powershell
go mod download
go mod verify
```

### 3. Сборка с помощью PowerShell скрипта

Проект включает PowerShell скрипт `build.ps1` для удобной сборки.

**Сборка для Windows:**
```powershell
.\build.ps1 windows
```

**Или просто:**
```powershell
.\build.ps1
```

**Другие варианты:**
```powershell
# Собрать только для Windows
.\build.ps1 windows

# Очистить директорию сборки
.\build.ps1 clean
```

**Если возникает ошибка "не удается загрузить, выполнение скриптов отключено":**

```powershell
# Разрешить выполнение скриптов (требуются права администратора)
Set-ExecutionPolicy RemoteSigned -Scope CurrentUser

# Или запустить один раз без изменения политики:
PowerShell -ExecutionPolicy Bypass -File .\build.ps1
```

### 4. Ручная сборка

Если предпочитаете собирать вручную:

```powershell
# Создать директорию для сборки
New-Item -ItemType Directory -Force -Path .\build

# Собрать для Windows
$env:CGO_ENABLED = "1"
go build -ldflags="-s -w" -o .\build\jotnal.exe .\cmd\ide
```

### 5. Результат сборки

После успешной сборки файл `jotnal.exe` будет находиться в директории `.\build\`

Размер файла: примерно 14 MB

---

## Запуск приложения

### Первый запуск

```powershell
cd .\build
.\jotnal.exe
```

При первом запуске:
- Автоматически создается конфигурационный файл в `%USERPROFILE%\.jotnal\config.json`
- Создается база данных SQLite в `%USERPROFILE%\.jotnal\jotnal.db`
- Добавляются начальные данные (должности, подразделения, пользователь DEVELOPER)

### Конфигурация

Файл конфигурации: `%USERPROFILE%\.jotnal\config.json`

Пример конфигурации:
```json
{
  "database": {
    "path": "C:\\Users\\YourName\\.jotnal\\jotnal.db",
    "password": "ваш_пароль"
  },
  "interface": {
    "theme": "dark",
    "font_size": 14,
    "window_size": {
      "width": 1280,
      "height": 720
    },
    "language": "ru"
  }
}
```

**Важно:** В Windows используйте двойные обратные слэши `\\` или прямые слэши `/` в путях.

### Расположение файлов

- Конфигурация: `C:\Users\<Имя>\\.jotnal\config.json`
- База данных: `C:\Users\<Имя>\\.jotnal\jotnal.db`

### Запуск из любой директории

Чтобы запускать `jotnal.exe` из любого места:

1. Скопируйте `jotnal.exe` в `C:\Program Files\Jotnal\`
2. Добавьте `C:\Program Files\Jotnal` в PATH
3. Теперь можно запускать командой `jotnal` из любой директории

---

## Решение проблем

### Проблема: "gcc: command not found" или "CGO disabled"

**Решение:**
- Убедитесь, что GCC установлен: `gcc --version`
- Проверьте PATH: `$env:PATH -split ';'` должен содержать путь к MinGW
- Перезапустите PowerShell после установки GCC
- Убедитесь, что `CGO_ENABLED=1` (по умолчанию включен на Windows)

### Проблема: "go: command not found"

**Решение:**
- Убедитесь, что Go установлен: `where.exe go`
- Добавьте `C:\Program Files\Go\bin` в PATH
- Перезапустите PowerShell

### Проблема: "Не удается загрузить build.ps1, выполнение скриптов отключено"

**Решение:**
```powershell
Set-ExecutionPolicy RemoteSigned -Scope CurrentUser
```

Или запустите один раз:
```powershell
PowerShell -ExecutionPolicy Bypass -File .\build.ps1
```

### Проблема: Ошибки при компиляции SQLCipher

**Решение:**
- Проверьте версию GCC: `gcc --version` (рекомендуется 12.x или выше)
- Убедитесь, что используете MinGW-w64 (не старый MinGW32)
- Попробуйте переустановить GCC через MSYS2

### Проблема: База данных не создается

**Решение:**
- Проверьте права доступа к `%USERPROFILE%\.jotnal\`
- Убедитесь, что путь в config.json корректный (используйте `\\` или `/`)
- Проверьте, что SQLCipher скомпилирован корректно

### Проблема: Долгая сборка при первом запуске

**Это нормально:**
- Первая сборка может занять 5-10 минут, так как компилируется SQLCipher
- Последующие сборки будут быстрее благодаря кешу Go

### Проблема: Приложение не запускается, нет ошибок

**Решение:**
- Запустите из PowerShell, чтобы увидеть ошибки: `.\build\jotnal.exe`
- Проверьте Windows Event Viewer (Просмотр событий)
- Убедитесь, что .NET Runtime 8.0 установлен (для WPF компонентов)

---

## Дополнительная информация

### Версионирование

Текущая версия: `1.0.0`

Для сборки с кастомной версией:
```powershell
$env:VERSION = "1.2.3"
.\build.ps1
```

### Разработка

Для разработки рекомендуется использовать:
- Visual Studio Code с расширениями Go и PowerShell
- GoLand от JetBrains
- Visual Studio 2022 (для WPF части)

### Тестирование

```powershell
# Запустить все тесты
go test ./...

# С выводом
go test -v ./...
```

### Отладка

Для запуска в режиме отладки:
```powershell
go run ./cmd/ide
```

---

## Быстрый старт (TL;DR)

```powershell
# 1. Установите Go, Git, и GCC (MinGW-w64)

# 2. Клонируйте репозиторий
git clone https://github.com/deldim-kam/Jotnal.git
cd Jotnal

# 3. Соберите проект
.\build.ps1

# 4. Запустите
.\build\jotnal.exe
```

---

## Полезные ссылки

- [Go Downloads](https://go.dev/dl/)
- [Git for Windows](https://git-scm.com/download/win)
- [MSYS2](https://www.msys2.org/)
- [TDM-GCC](https://jmeubank.github.io/tdm-gcc/)
- [Документация Go](https://go.dev/doc/)
- [SQLCipher](https://www.zetetic.net/sqlcipher/)

---

## Поддержка

При возникновении проблем:
1. Проверьте раздел [Решение проблем](#решение-проблем)
2. Создайте Issue на GitHub
3. Свяжитесь с командой разработки

---

**Успешной работы с Jotnal! 🚀**
